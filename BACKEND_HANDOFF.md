# 프론트엔드 진행 상황 정리 (백엔드 전달용)

작성일: 2026-09-15 · 대상 저장소: `Mungsmu_FE` (웹, 저장소 루트) + `mobile/` (Expo)

한 줄 요약: **웹은 인증 5개 API만 실제 백엔드(Ma_BE)에 붙어 있고, 그 외 모든 기능(경로탐색·지도·코스/터널 데이터·마이페이지·보호자 알림)은 백엔드 없이 동작 중입니다. 모바일은 인증 포함 백엔드 연동이 0%로, 전부 로컬 목업입니다.**

---

## 1. 저장소 구조

| 위치 | 스택 | 비고 |
|---|---|---|
| 저장소 루트 | React 18 + Vite + react-router-dom | 웹 앱, 기능이 가장 앞서 있음 |
| `mobile/` | Expo (React Native) | 별도 `package.json`/`.env`, 웹 화면을 수동으로 포팅 중. 화면 구성은 웹과 거의 동등해졌지만 **백엔드 연동은 아직 시작 전** |

두 앱은 코드 공유가 없어서, 아래 "완료/미완료" 상태가 웹과 모바일에서 서로 다릅니다.

---

## 2. 웹 — 백엔드와 이미 연동된 부분

`src/lib/auth.js`가 Ma_BE(`:8081`, dev 중 Vite가 `/api`를 프록시)를 호출합니다. 아래 5개 엔드포인트는 **이미 프론트가 맞춰서 구현 완료**한 상태이므로, 스펙을 바꾸면 프론트도 같이 수정해야 합니다.

| 엔드포인트 | 메서드 | 요청 body | 사용처 |
|---|---|---|---|
| `/members/check-username?username=` | GET | - | 회원가입 아이디 중복확인 |
| `/sms/send` | POST | `{ phone }` | 회원가입 SMS 인증번호 발송 |
| `/sms/verify` | POST | `{ phone, code }` | SMS 인증번호 확인 |
| `/members/signup` | POST | `{ name, phone, email, username, password, passwordConfirm, guardianName, guardianPhone, guardianRelation }` | 회원가입 |
| `/auth/login` | POST | `{ username, password }` → 응답 `data.accessToken` 사용 | 로그인, JWT 발급 |
| `/members/me` | GET (Bearer 인증) | - | 로그인 직후 내 정보 조회 |

- 공통 응답 포맷 가정: `{ success, message, data }`. `success === false`면 프론트는 `message`를 에러로 표시합니다.
- 세션은 `localStorage`(`maeum-sumgil:session`)에 `{ accessToken, ... }`로 저장, 이후 요청은 `Authorization: Bearer <token>` 헤더로 붙입니다.
- 비밀번호 규칙은 프론트에서 `영문+숫자 포함 8자 이상`으로 백엔드 `SignupRequest`와 맞춰뒀다고 코드 주석에 명시돼 있음 — 백엔드 쪽 규칙이 바뀌면 알려주세요.

## 3. 웹 — 아직 백엔드가 없는 부분 (전부 프론트 로컬/하드코딩)

로그인 이후 화면들은 `getMe()` 응답을 쓰지 않고 여전히 정적 값입니다:

- **마이페이지** (`src/pages/MyLoginPage.jsx`): 프로필 이름("서연 님"), 이번 달 통계(터널 통과 12회 / 회피 5회 / 평균 난이도 3단계), 보호자 카드("김민준", 연락처)가 전부 하드코딩. "프로필 수정", "연락처 변경", "이용 내역", "앱 설정", "문의하기" 버튼은 클릭해도 동작 없음.
- **보호자 알림 3개 토글**(실시간 위치 공유 / 자동 통과 알림 / 긴급 호출 위임): 로컬 `useState`만 있고 서버 저장도, 실제 SMS/알림 발송도 없음. ⚠️ 회원가입 완료 화면에서는 "길찾기 시 보호자 OO님께 SMS로 알려드릴게요"라고 이미 사용자에게 안내하고 있어서, **실제 발송 기능이 우선순위가 높습니다.**
- **코스/터널/지역 데이터** (`src/data/mock.js`, `gangwonGeo.js`): 안심 코스 4건, 터널 6건, 18개 시군 등급 전부 하드코딩 배열. 조회 API 없음.
- **경로탐색은 실제로 동작하지만 자체 백엔드를 거치지 않습니다** — 프론트가 공개 Valhalla 라우팅 서버(`valhalla1.openstreetmap.de`, 무료/무키)를 직접 호출해 최단 경로와 "터널 완전 회피" 경로를 계산합니다 (`src/lib/route.js`). 즉 백엔드가 별도 경로탐색 API를 만들 필요는 없지만, **공개 서버라 상용 트래픽에는 부적합** — 자체 라우팅 서버 호스팅 필요 여부는 논의가 필요합니다.
- **지도**: Kakao Maps JS SDK를 프론트에서 직접 로드 (`VITE_KAKAO_MAP_KEY`). 백엔드 경유 없음.

## 4. 모바일 — 백엔드 연동 현황: 0%

`mobile/src/lib/auth.js`는 웹과 함수 이름/세션 키는 똑같이 맞춰뒀지만, 내부는 전부 `AsyncStorage` 목업입니다. 실제 서버 호출이 전혀 없습니다.

| 화면 | 현재 동작 |
|---|---|
| `LoginScreen` | 가입 기록이 로컬(AsyncStorage)에 있으면 비밀번호 대조, 없으면 입력값 그대로 임시 세션 생성 — **서버 로그인 아님** |
| `SignUpScreen` | 아이디 중복확인 = 로컬 목록 대조 (`setTimeout`으로 로딩만 흉내), SMS 인증 = 실제 발송/검증 없이 **아무 6자리나 입력하면 통과**, 가입 = `AsyncStorage`에 저장 |
| 비밀번호 규칙 | 8자 이상만 체크 — 웹/백엔드의 "영문+숫자 포함 8자" 규칙과 다름 (모바일에 실 연동 붙일 때 맞춰야 함) |

즉 **모바일에는 웹의 5개 인증 API를 이식하는 작업이 아직 시작되지 않은 상태**입니다.

---

## 5. 백엔드에 확인/요청 드리고 싶은 사항 (우선순위 순)

1. **보호자 알림 발송 API** — 동반 모드 시작/터널 통과 시 보호자에게 SMS(또는 푸시) 발송. 현재 프론트 어디에도 없는데 사용자에게는 이미 있다고 안내 중이라 우선순위 높음.
2. **모바일 인증 연동** — 웹과 동일한 5개 API(§2)를 모바일에서도 호출 예정. CORS/모바일 클라이언트 인증 헤더 등 웹과 동일하게 동작하는지 확인 필요.
3. **마이페이지 데이터 API** — 프로필 조회(이미 있는 `/members/me` 응답에 통계/보호자 필드 포함 가능한지), 보호자 CRUD, 설정 토글 3종 서버 저장.
4. **통계 API** — 이번 달 터널 통과/회피 횟수, 평균 난이도. (지금은 하드코딩)
5. **코스/터널/지역 데이터 API** — 지금 프론트 배열을 대체할 CRUD. 급하지 않다면 현행 유지 가능.
6. **경로탐색 자체 호스팅 여부** — 공개 Valhalla 서버를 계속 쓸지, 백엔드에서 라우팅 엔진을 자체 호스팅할지 결정 필요 (트래픽 늘어나기 전에 논의 권장).

---

## 참고

- 웹 라우팅/내비게이션 기능의 기술적 디테일(Valhalla 옵션, 카카오맵 연동 방식 등)은 [README.md](../README.md) 참고.
- [PROJECT_OVERVIEW.md](PROJECT_OVERVIEW.md)는 이 저장소 초기(mock 전용) 상태 기준으로 작성된 옛 문서라 위 내용과 다른 부분이 있습니다 — 최신 기준은 이 문서와 README.md입니다.
